### a2a_chat:
Chat with any FastA2A-compatible remote agent. Context preserved automatically across calls.

**Args:** `agent_url` (host:port or full URL), `message` (string), `attachments` (list, optional), `reset` (bool — true starts fresh conversation)

Full docs: cat /a0/agents/agent0/prompts/tool-docs/a2a_chat.md
