### notify_user:
Send a notification to the user independent of current task output.

**Args:** `message` (required), `title` (optional), `detail` (optional, html ok), `type` (optional: info success warning error progress)

Full docs: cat /a0/agents/agent0/prompts/tool-docs/notify_user.md
